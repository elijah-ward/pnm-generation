// main.c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(int argc, char **argv) {
    int flags, opts;
    int type, height, width, format;
    char **output_name;

    for ( idx=0; idx < argc; idx++){
        puts(argv[idx]);
    }
}

